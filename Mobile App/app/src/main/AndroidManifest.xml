<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brad.mansfieldapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="KPI Tracker"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimescaleSelection" />
        <activity android:name=".ProcessSale" />
        <activity android:name=".LoginScreen" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".KPISubSelection"/>
        <activity android:name=".KPISelection"/>
        <activity android:name=".PersonalTracker"/>
        <activity android:name=".StoreTracker"/>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>